
from integralstor import db

def get_file_info_row(db_path, path):
    row = False
    try:
        query = 'select * from file_info where path="%s"'%path
        row, err = db.get_single_row(db_path, query)
        if err:
            raise Exception(err)
    except Exception, e:
        return None, 'Error retrieving file info row : %s'%str(e)
    else:
        return row, None
